{
  "name": "anycontrol-remote-workspace",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:agent": "npm run dev --prefix agent -- --demo",
    "dev:frontend": "npm run start --prefix frontend"
  },
  "devDependencies": {
    "@aws-amplify/backend-cli": "^1.2.0",
    "concurrently": "^10.0.5",
    "esbuild": "0.28.2"
  }
}
